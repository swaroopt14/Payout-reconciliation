# Local dev (copy to .env.local). Docker Compose uses env.docker-compose.example instead.
# Cash/settlement routes return 503 unless ONE of these is set:
ZORD_SETTLEMENT_URL=http://localhost:8081
# Demo only - fixture data labelled demo; NEVER use for a live tenant (leave ZORD_SETTLEMENT_URL unset):
# SMOKE_SIMULATOR_URL=http://localhost:8099
# CLEARLINE_DEMO_SETTLEMENT_SIMULATOR=1
