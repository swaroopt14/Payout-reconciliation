# Clearline lessons

One line per HOLD or bug. Read this and `docs/clearline-decisions.md` before building or reviewing anything.

Add a line whenever a review HOLDs or a bug is found. Every lesson needs an owner, the rule, and the test that guards it. A lesson with no guarding test is unfinished, so mark it `TODO` until the test exists.

| # | Date | Owner | What went wrong | Rule | Guarding test |
|---|------|-------|-----------------|------|---------------|
| L1 | 2026-09-25 | Go / Finance | Payout de-duplication keyed on the amount, and expected refunds could double up | De-duplicate money items by their own id (payout ref, refund id), never by amount | TODO (Slice 8): payout dedup test that changes the amount on the same ref; expected-refund dedup by refund id |
| L2 | 2026-09-25 | Architect / all | Features were called "wired" when the config switched them off (`dispatch.enabled: false`, `router_url` unset, recon listening on topic `z.dispatch.events.v1` while relay publishes `payments.dispatch.events.v1`) | Read the config and compose files before claiming something is wired, and prove it with one end-to-end run | TODO (Slice 8): Test's end-to-end run from payout file through router, relay, recon, database and API |
| L3 | 2026-09-25 | Go | Relay converted rupees to paise through float64 (`dispatch_loop.go:899`), and some tables stored rupees as NUMERIC | No float anywhere in a money path. Amounts are int64 paise end to end | TODO (Slice 8): conversion is exact for ₹0.01 and ₹1,00,000.10, plus a migration test |
| L4 | 2026-09-25 | Go / Staff | A same-amount debit could be MATCHED at 0.99 confidence with no reference (`financial_service.go:809`) | MATCHED needs a UTR or reference plus the amount. Amount-only is AMBIGUOUS at most | TODO (Slice 8): a same-amount debit with no reference never reaches MATCHED |
| L5 | 2026-09-25 | Go / Finance | Reversed payouts still counted as projected debits (`razorpay/status.go:61`, `books.go:272`) | A reversed or failed payout is never an expected debit, and a pending one stays expected until the bank debit or a confirmed failure | TODO (Slice 8): reversed, failed and pending payout cases in Test's end pack |
| L6 | 2026-09-25 | Go | Relay treated a rupee amount as paise, so a payout could go out at the wrong size | Name every money field in minor units (`_minor`, `_paise`) and convert only at the edge, with a unit check | TODO (Slice 8): relay amount test, where ₹1 becomes exactly 100 paise on the provider request |
| L7 | 2026-09-25 | Go / Cloud | `dispatch_index` was always empty because its UUID `connector_id` column rejected `'razorpayx-v1'`, so no row was ever written | Check that an index recon depends on is actually filled before relying on it, not just that the code writes to it | TODO (Slice 8): a relay dispatch writes a `dispatch_index` row that recon can link |
| L8 | 2026-09-25 | Marketplace | Refunds with no seller_id on multi-seller payments were given the first seller, so refunds landed on the wrong seller | Never guess a seller on a multi-seller payment. Leave it empty | `TestSellerIDFromTransfers_MultiSellerLeavesEmpty` |
| L9 | 2026-09-25 | Go / Connectors | Edge webhook verification fell back to the global `RAZORPAY_WEBHOOK_SECRET` when a tenant had no secret, and recon `EnvCredentialResolver` gave every tenant the shared `RAZORPAY_KEY_*` pair | No shared fallback: a tenant with no own secret fails closed (`ErrNoTenantSecret`, webhook 401, nothing processed) | `TestWebhook_TenantWithoutSecretRejectedEvenWithGlobalEnv`, `TestResolveWebhookSecret_NoTenantSecretRejectedEvenWithGlobalEnv`, `TestEnvCredentialResolver_TenantWithoutSecretRejectedEvenWithGlobalEnv`, `TestBackfill_TenantWithoutSecretFailsClosedNoProviderCall` |
| L10 | 2026-09-25 | Go / Connectors | `connectors.secret` held webhook secrets in plaintext | Per-tenant secrets are written only as `enc:v1:` (AES-256-GCM, `CLEARLINE_SECRETS_KEY`); legacy plaintext is rejected on read and sealed by `cmd/reencrypt-secrets` | `TestSetWebhookSecret_StoredValueIsEncryptedNotPlaintext`, `TestWebhook_LegacyPlaintextSecretRejected`, `TestReEncryptConnectorSecrets_LegacyRowsSealedIdempotent`, secretbox tests |
| L11 | 2026-09-25 | Go / Security | A real `rzp_test_` key pair was committed in a plan doc and a unit test | Only obvious fakes (`rzp_test_FAKEKEYID`) in code and docs; real keys come from env and tests skip when unset | TODO: repo secret-scan check in CI |
